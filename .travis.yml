language: php
php:
  - 7.1.9


before_script:
  - cp .env.travis .env
  - mysql -e 'CREATE DATABASE IF NOT EXISTS apitest;'
  - composer self-update
  - composer install --no-interaction

script:
  - vendor/bin/phpunit